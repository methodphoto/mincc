const { createGamePages } = require("./games");
const { createFixturePages } = require("./fixtures");
const { createStatisticsPages } = require("./statistics");

module.exports = {
  createGamePages,
  createFixturePages,
  createStatisticsPages,
};
