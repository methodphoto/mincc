const { createGamePages } = require("./games");
const { createFixturePages } = require("./fixtures");
const { createStatisticsPages } = require("./statistics");
const { createRecordsPages } = require("./records");
const { createResultsPages } = require("./results");

module.exports = {
  createGamePages,
  createFixturePages,
  createStatisticsPages,
  createRecordsPages,
  createResultsPages,
};
